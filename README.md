# Neural-Network
My first attempt at coding a neural network!
My first attempt at coding a neural network! Uses a neural network to figure out whether or not an input vector is above a similarity threshold to one pre-determined vector. not the most accurate, but it does some interesting things :)

# sample data: